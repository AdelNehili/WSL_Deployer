version: "3.9"

services:
  pixel-generator:
    build: ./app
    container_name: pixel_generator_cpu
    ports:
      - "8000:8000"
    environment:
      - DEVICE=cpu
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2g